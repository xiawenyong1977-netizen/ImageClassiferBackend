# FastAPI框架和ASGI服务器
fastapi==0.109.0
uvicorn[standard]==0.27.0
gunicorn==21.2.0

# 数据库
pymysql==1.1.0
aiomysql==0.2.0
cryptography==42.0.0

# 数据验证和模型
pydantic==2.5.3
pydantic-settings==2.1.0

# 图片处理
Pillow==10.2.0

# 二维码检测
pyzbar==0.1.9

# ONNX Runtime (本地模型推理)
onnxruntime==1.16.3
numpy==1.24.3

# PyTorch CPU 版本（服务器无GPU，使用CPU版本节省空间）
# 注意：需要在安装其他依赖前先安装CPU版本的PyTorch，避免ultralytics安装CUDA版本
# 安装命令：pip install torch==2.1.0 torchvision==0.16.0 --index-url https://download.pytorch.org/whl/cpu
# 这里不直接列出，因为requirements.txt不支持--index-url参数
# torch==2.1.0
# torchvision==0.16.0

# YOLO推理库（自动处理YOLO预处理和后处理）
ultralytics==8.0.200  # 包含PyTorch依赖，但如果已安装CPU版本，会使用已安装的版本
# 注意：ultralytics会自动检测已安装的torch，如果已安装CPU版本则不会重新安装CUDA版本
# opencv-python==4.8.1.78  # 可选：OpenCV DNN模块

# 大模型API客户端
dashscope>=1.14.0      # 阿里云灵积DashScope SDK
openai==1.10.0
# anthropic==0.15.0  # 暂时注释，如果需要Claude再安装

# 工具库
python-multipart==0.0.6  # 文件上传
pypinyin>=0.50.0         # 中文转拼音（数据库工具脚本用）
python-dotenv==1.0.0      # 环境变量
loguru==0.7.2             # 日志
scikit-learn>=1.3.0       # DBSCAN聚类算法（V3逆地址编码接口需要）

# 认证和安全
python-jose[cryptography]==3.3.0  # JWT token
passlib[bcrypt]==1.7.4            # 密码哈希

# HTTP客户端
httpx==0.26.0
aiofiles==23.2.1

# 七牛云存储SDK
qiniu==7.17.0

# 测试工具（可选）
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0

